/*finding the approximate value of e^x*/

#include<stdio.h>
float Exp(float x, float n);
int main()
{
	float x;
	float n;
	float result;
	printf("input x and n: ");
	scanf("%f %f ", &x, &n);
	
	printf("exp(%f) = %f\n",x,result); 
} 

float Exp()
{
	float sum;

	return sum;
}
