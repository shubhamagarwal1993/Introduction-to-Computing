/*Arcsin Computation*/

#include<stdio.h>

float TermArcsin (/* arguments for the function */);
float Arcsin (/* arguments for the function */);

int main ()
{
	float x;
	float n;
	float result;
	printf("input x and n: ");
	scanf("%f %f", &x, &n);
	/* result = Arcsin(<<arguments>>); */
	printf("arcsin(%f) = %f\n",x, result);
}


float Arcsin (/* arguments for the function */)
{
	float sum; 
	/* implement the Arcsin computation using calls to TermArcsin */
	return sum; 
}

float TermArcsin (/* arguments for the function */) 
{
	/* implement computation of kth term using power & factorial functions */
}

float factorial(float in)
{
	for (int i = 1; i<in; i++)
	{
		i = i*in
	}
	return 
}
